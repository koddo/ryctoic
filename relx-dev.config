%% -*- mode: erlang; -*-

%% {dev_mode, true}.
{release, {hello_world, "1"},
 [hello_world
  %% syntax_tools, compiler, sync   % for on-the-fly reloading code, I disabled it --- https://github.com/rustyio/sync/issues/59
 ]}.
{extended_start_script, true}.
{include_erts, false}.
{include_src, false}.

{vm_args, "./etc/vm.args"}.
{sys_config, "./etc/sys.config"}.


%% {overlay, [{copy, "etc/sync.config", "etc/sync.config"}]}.
