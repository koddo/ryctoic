

# not finished yet


RUN apt-get update && apt-get install -y \
    foreachq(`package', `curl, wget`, `package \`)
    && \
    apt-get update && apt-get install -y erlang=$ERLANG_VERSION && \
    apt-get -y autoclean && apt-get -y autoremove


