<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">  
        <title>log in</title>
        <!--[if lt IE 9]><script src="js/html5shiv-printshiv.js" media="all"></script><![endif]-->
    </head>
    <body>
        <!-- This is a VERY bare bones version of a HTML5 template! -->

        <h1>you are logged in</h1>
        <p>continue to the app</p>
        <button onclick='closeThisPopupAndGoto("/"); window.close();'>app</button>
        <p>TODO: make this popup fancy -- user can see this if the opener is closed</p>



        <script>
         var mystr = "{{ id }}, {{ email }}, {{ email_verified }}";
          
         if (window.opener && "" != window.opener.location) {
             window.opener.myhandler(mystr);
             window.close();
         }


         function closeThisPopupAndGoto(url) {
             var newWindow = window.open(url);
             if (window.focus) {
                 newWindow.focus();
             }
         }
        </script>
    </body>
</html>
